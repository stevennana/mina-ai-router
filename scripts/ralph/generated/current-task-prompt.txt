Read the following repository documents before making changes:
- AGENTS.md
- docs/FRONTEND.md
- docs/product-specs/agent-capability-refresh.md
- docs/design-docs/collaboration-reliability.md
- docs/exec-plans/active/005-capability-freshness-ui.md

Current task id: capability-freshness-ui
Current task title: Capability freshness UI

Objective:
Show capability freshness in the browser console and expose a safe refresh/edit path.

Scope:
- Display missing/stale/fresh/manual states in the inspector.
- Provide a refresh affordance if the API exists from the prior task.
- Preserve manual editing through `Edit Capabilities`.
- Verify desktop and mobile layout does not overflow.

Out of scope:
- New CLI command behavior.
- Scheduled refresh.
- Team-wide capability comparison.

Exit criteria:
1. Users can tell whether an agent capability card is fresh enough to trust.
2. Manual and generated capability summaries are not visually confused.
3. Focused verification proves the relevant UI/API data is exposed and docs links remain valid.
4. UI build, HTTP smoke, and docs smoke pass.

Required checks:
- `npm run build`
- `npm run smoke:http`
- `npm run smoke:docs`

Evaluator notes:
This is a UI task. Require visual sanity in the inspector and activity layout before promotion.

Required repository artifacts for this task:
- apps/http-server/ui/src/features/Inspector.tsx
- apps/http-server/ui/src/features/AgentDetailsForm.tsx

Required commands for this task:
- npm run build
- npm run smoke:http
- npm run smoke:docs

Previous known gaps from the last evaluation:
- No visual sanity evidence for the inspector and activity layout, despite evaluator notes requiring it before promotion.
- No desktop and mobile overflow verification evidence for floating panels.
- No focused UI/DOM/screenshot assertion proving the new freshness states render in the browser console.
- The worker handoff explicitly says browser screenshots could not be captured, so UI verification was limited to build and static CSS/layout inspection.



Rules:
- Work only on this task.
- Do not broaden scope.
- Keep the existing package manager.
- Preserve repository abstractions.
- Keep changes deterministic and minimal.
- Update this task doc's progress log with concrete notes.
- Keep the repository in a state where the required commands can pass.
- If the task is not done, leave the next task untouched.


At the end of the run, write a concise operator handoff summary into state/last-result.txt via your final response:
- what changed
- whether the exit criteria appear satisfied
- what remains if the task is still incomplete
